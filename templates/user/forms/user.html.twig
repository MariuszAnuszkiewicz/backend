{{ form_start(form, {'method': 'POST'}) }}
<input type="hidden" name="token" value="{{ csrf_token('user_item') }}"/>
<label for="user" class="control-label"><strong class="text-black-50">{{ form_label(form) }}</strong></label>
<div class="form-control bg-light">
    <div class="mb-3">
        <strong>{{ form_widget(form) }}</strong>
    </div>
</div>
{{ form_end(form) }}
